import { Layout } from 'antd'
import './index.less'

const LayoutHeader = () => {
  const { Header } = Layout
  return (
    <Header>
      <div style={{ color: '#fff' }}>头部</div>
    </Header>
  )
}

export default LayoutHeader
