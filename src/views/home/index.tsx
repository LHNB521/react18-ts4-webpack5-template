import MapIndex from '@/components/MapIndex'
import styles from './index.scss'

const home: React.FC = () => {
  return (
    <div className={styles.home}>
      <MapIndex />
    </div>
  )
}
export default home
